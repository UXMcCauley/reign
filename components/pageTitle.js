
export default function PageTitle(props) {
    return (
        <div className={""}>
            <div className={""}>
                <h1>{props.label}</h1>
            </div>
        </div>
    )
}
